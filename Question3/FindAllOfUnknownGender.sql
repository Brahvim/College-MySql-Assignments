SELECT *
FROM soc_net_db.users_table
WHERE gender IS NULL;
-- This shows user7.
-- Other states we can check for are:
-- `IS FALSE`,
-- `IS TRUE`,
-- `IS NULL`,
-- `IS NOT FALSE`,
-- `IS NOT TRUE`,
-- `IS NOT NULL`.